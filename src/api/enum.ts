export enum GlobalState {
  ENABLED = 10,
  DISABLED = 20,
  DESTROYED = 30
}

export enum DeviceState {
  iPhone = 10,
  Android = 20,
  Windows = 30,
  Unknown = 100
}

export enum PaginationState {
  PAGE = 1,
  LIMIT = 10,
  SORT_BY = 'id',
  SORT_TYPE = 'ASC'
}
